discovery:
  seed_hosts:
    - "127.0.0.1"

cluster:
    initial_master_nodes: 
    - "netology_test"

node:
  name: netology_test

network:
  host: 0.0.0.0

path:
  data: /var/lib/elasticsearch
  logs: /var/lib/elasticsearch